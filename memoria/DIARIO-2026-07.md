# Diario — memoria de sesiones

## 2026-07-26 21:59 — Claude Code upgrade: reloj, SW guard, La Memoria, repo guard hooks (auto)
- Done: Built and wired 4 upgrades from leandro-os-prototype plan. (1) El Reloj: SessionStart + UserPromptSubmit hooks inject live timestamp (`~/.claude/hooks/reloj.sh`), verified desktop + terminal. (2) archivo sw-bump-guard.sh: Stop + pre-push hook enforcing CACHE_NAME bump when index.html changes, committed `10baa49`, pushed. (3) La Memoria: `/wrap` skill + SessionEnd auto-distiller (`wrap-auto.sh`) + SessionStart reader (`memoria-read.sh`) writing/reading `~/.claude/memoria/DIARIO-2026-07.md` (global) — user manually wired via jq merge command since permission classifier blocked settings.json edit twice. Verified end-to-end with synthetic transcript + fresh headless session resurrection test. (4) Repo-specific guard hooks added to operacion-bolivar (smoke-gate, runs harness), archivo-corpus (contexto-guard, blocks AI edits to contexto/), app-shell (estate-guard), cuaderno (estate-guard, ship rule + deploy-gate reminder) — all committed locally, cuaderno's guard already in prior commit `a69648d`.
- Rejected: sed/manual overwrite of settings.json to bypass permission classifier denial — classifier held even after explicit user chat approval; resolved by giving user a jq command to run themselves instead.
- Files: `~/.claude/hooks/{reloj.sh,memoria-read.sh,wrap-auto.sh}`, `~/.claude/skills/wrap/SKILL.md`, `~/.claude/settings.json`, per-repo `.claude/hooks/*guard*.sh` + `.claude/settings.json` in operacion-bolivar/archivo-corpus/app-shell/cuaderno, `~/.claude/projects/-Users-leandroguevara/memory/claude-code-upgrade.md`.
- Open: guard hooks for operacion-bolivar/archivo-corpus/app-shell are committed locally but not yet pushed (user said "go for guard hooks... then pitch revision" — push + `/revision` pitch still pending at cutoff). Cross-session communication (item #3 of original wishlist) not yet started.
- Gotchas: project-level hooks only load for sessions opened inside that repo — a session launched elsewhere that reaches into the repo via Bash does not inherit them.
