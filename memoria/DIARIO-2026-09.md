# Diario — memoria de sesiones

## 2026-09-11 10:59 — El coach de La Forja pedía API key con el helper vivo al lado: `enabled` bloqueaba la ruta Max
- Done: Diagnosticado y arreglado en `~/Projects/cuaderno`. `isClaudeEnabled()` ANDeaba `state.settings.claude.enabled`, que SOLO escribe el botón Save-key — así que en un dispositivo donde nunca se pegó una clave la ruta de suscripción era inalcanzable POR CONSTRUCCIÓN, con `/pair` devolviendo token a 20cm de distancia. El comentario encima de esa línea ya decía lo contrario desde hace meses. `enabled` pasa a guardar solo la ruta de API (dentro de `canUseApi`), `isClaudeEnabled() = canUseApi() || canUseSub()`. 6 líneas nuevas en el harness (incluida una que prueba que el selector de Billing se sigue respetando) + control rojo; FALSIFICADO re-plantando el gate original en una copia scratch servida en su propio puerto → 2 fallos nombrados. Gate 72 PASS / 0 FAIL. Commits `5ec1cd3` y `2f04d13`, pusheados; Pages verificado por bytes SERVIDOS (los intentos 1-2 aún servían el viejo). Leandro confirma: el coach corre.
- Rejected: arreglar los otros cuatro forks (lampara · bitácora · archivo · app-shell tienen el gate IDÉNTICO) — Leandro: el coach solo existe en cuaderno y el resto anda. Queda latente, no arreglado. También: verificación end-to-end contra el origen real de Pages por el Browser pane — navegación denegada; caí a bytes servidos + sonda CORS con control negativo.
- Files: `cuaderno/index.html` (el gate + el comentario que ahora coincide con el código) · `cuaderno/test-harness.html` (6 líneas `gate:` + control) · `cuaderno/sw.js` (`CACHE_NAME` v11→v12) · `cuaderno/CLAUDE.md` (la regla nueva).
- Open: la forma `enabled &&` sigue viva en los otros cuatro forks — muerde en cuanto uno pierda su clave o estrene dispositivo. · El modelo de La Forja está en Sonnet: pregunté si es deliberado o deriva, sin respuesta. · Nunca observé el coach habilitado en el deploy real con mis propios ojos: tengo los bytes y el grant CORS, la confirmación es de Leandro.
- Gotchas: **Verifiqué en MI localhost y dejé el arreglo sin commitear** — él siguió viendo el bug y el veredicto correcto era `curl` a Pages + grep de los bytes servidos, nunca mi propio puerto. El service worker (offline-first, cachea `index.html`) habría producido el MISMO síntoma aun con el push correcto: un push que falta y un SW rancio son byte-idénticos. · `grep -c "canUseApi() || canUseSub()"` es un discriminador CIEGO: el gate viejo contiene esa subcadena también, así que mi primer chequeo del re-plante cantó "files identical" sobre dos archivos que sí diferían. · La sonda CORS del helper necesita control negativo: el origen de Pages recibe `Access-Control-Allow-Origin`, `localhost:8731` recibe 200 SIN header — sin el control, el 200 se lee como permiso.
